{{#if posts.length}}
<section>
      <h2>Your Posts</h2>
      {{#each posts as |post|}}
      {{> post-info post}}
      {{/each}}
</section>
{{/if}}
<a class="link" href="/dashbboard/new">+ New Post </a>
<button type="submit" class="btn"></button>
<script src="/javascript/add-post.js"></script>